id: https://example.org/widget
name: widget
prefixes:
  linkml: https://w3id.org/linkml/
  ex: https://example.org/
default_prefix: ex
default_range: string
imports:
  - linkml:types
classes:
  Widget:
    tree_root: true
    attributes:
      id:
        required: true
      label:
        required: true
